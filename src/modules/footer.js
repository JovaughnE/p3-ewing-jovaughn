class Footer { // create a class for the Footer
    constructor(footerContent) {
        this.footerContent = footerContent;

    }
}

export const footer = new Footer ('Footer Content!'); //export the new class Footer as a variable

