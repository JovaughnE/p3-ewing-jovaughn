class Header { // create a class for the header
    constructor(title) {
        this.title = title;

    }
}

export const header = new Header('My food app'); //export the new class header as a variable

//export const nav = new Navigation(''); // Delete if not used